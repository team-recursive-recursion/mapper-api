@model IEnumerable<Mapper_Api.Models.Point>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CourseID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.GolfCourseID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.HoleID)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GolfCourseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoleID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseElementID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseElementID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseElementID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
